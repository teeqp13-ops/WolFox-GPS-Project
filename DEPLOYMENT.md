# دليل تجهيز ونشر WolFox GPS

## المتطلبات
- PHP 8.1 أو أحدث مع امتدادات PDO_SQLite وcURL.
- HTTPS مفعل على النطاق.
- صلاحية كتابة لمجلد `backend/` لإنشاء قاعدة SQLite.
- بيئة Theos/macOS منفصلة لبناء حزمة iOS.

## متغيرات بيئة الخادم
اضبط هذه المتغيرات في إعدادات PHP-FPM أو لوحة الاستضافة، ولا تكتبها في Git:

- `WOLFOX_ADMIN_PASSWORD`: كلمة مرور لوحة الإدارة.
- `OPENAI_API_KEY`: اختياري؛ مطلوب فقط لصفحة مساعد المشروع.
- `OPENAI_MODEL=gpt-5.6-sol`: نموذج المساعد.

ملف `.env.example` مرجع للأسماء فقط؛ لا يحمّل PHP ملفات البيئة تلقائيًا.

## النشر
1. ارفع مجلد `backend/` خارج المسار العام إن أمكن، أو امنع الوصول المباشر إلى `database.sqlite`.
2. اضبط متغيرات البيئة وأعد تشغيل PHP-FPM عند الحاجة.
3. افتح `backend/admin/index.php` وسجل الدخول.
4. أنشئ كود اختبار ثم فعّله من جهاز مصرح به.
5. افتح صفحة «مساعد المشروع» وتحقق من تحليل نص تجريبي غير حساس.

## التحقق قبل الإصدار
- تأكد أن طلب GitHub #5 أخضر.
- ابنِ حزمة Theos في بيئة متوافقة.
- ثبّت الحزمة على جهاز اختبار مصرح به فقط.
- أنشئ SHA-256 للحزمة الناتجة وسجله في ملاحظات الإصدار.
