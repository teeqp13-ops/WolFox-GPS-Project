# التحقق والأمان

## إعداد الخادم
- اضبط كلمة مرور الإدارة كمتغير بيئة باسم `WOLFOX_ADMIN_PASSWORD`.
- لا تضع كلمات المرور أو مفاتيح خاصة في المستودع أو داخل تطبيق iOS.
- تأكد أن `backend/database.sqlite` قابل للكتابة على الخادم فقط وغير متاح للتحميل عبر الويب.

## التفعيل
واجهة التفعيل ترسل الكود ومعرّف الجهاز عبر HTTPS. لا يحتوي العميل على سرّ مشترك يمكن استخراجه من الحزمة.

## التحقق الحالي
يتحقق GitHub Actions من صياغة PHP ووجود ملفات مشروع Theos وعدم وجود كلمات مرور مدير ثابتة أو مفاتيح HMAC مضمّنة. بناء حزمة Theos يحتاج بيئة macOS/Theos متوافقة، لذلك لا يدّعي هذا المستودع نجاح بناء حزمة عند عدم توفر تلك البيئة.

## قبل الإصدار
1. اضبط متغير البيئة على الاستضافة.
2. نفّذ اختبار تفعيل بكود تجريبي وعلى جهاز مصرح به.
3. ابنِ الحزمة في بيئة Theos متوافقة.
4. أنشئ SHA-256 للحزمة وسجّل نتيجة الاختبار في الإصدار.
